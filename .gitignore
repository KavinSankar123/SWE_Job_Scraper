# Secrets — the private launchers hold your Gmail App Password
run.sh
run_tech.sh
run_newgrad.sh

# Google service-account keys. These belong outside the repo entirely
# (see tech/SHEETS_SETUP.md); this is only a backstop against a stray `git add`.
*service-account*.json
gsheet-*.json

# Local tooling / editor
.claude/
.DS_Store

# Python virtual environment
.venv/
__pycache__/
*.pyc

# Runtime state, logs, and debug dumps
# (*.sqlite3* also covers the -wal / -shm sidecars WAL mode creates)
*.sqlite3*
*.log
cron.log
launchd.*.log
debug_*.html
watcher.out
